# NOTE: requires Python >= 3.12 — pandas-ta only publishes 0.4.x wheels with
# Requires-Python >=3.12 (all older versions were removed from PyPI).
# CI (.github/workflows/daily_signals.yml) runs 3.12 for this reason.

# Phase 01 — Data Layer
requests
pandas
ccxt
pandas-datareader
pyarrow
pytest
yfinance

# Phase 02 — Feature Engineering
pandas-ta>=0.4.71b0
hmmlearn
scipy
numpy
xgboost
shap

# Phase 03 — ML Modeling
lightgbm
torch
optuna
mlflow
scikit-learn
joblib

# Phase 04 — Backtesting
quantstats

# Phase 06 — Strategy Research
statsmodels

# Phase 06E — Live Signal Engine
alpaca-trade-api

# Phase 07 — Dashboard
streamlit
plotly
